import React from "react";

// This will render shows you are currently watching because don't we all lose track?

const CurrentlyWatching = () => {
  return <div>this is the currently watching component</div>;
};

export default CurrentlyWatching;
