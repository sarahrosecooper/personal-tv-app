import React from "react";

// This will render shows you haven't finished because you didn't like them or that you didn't like

const Disliked = () => {
  return <div>this is the disliked component</div>;
};

export default Disliked;
