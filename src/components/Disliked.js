import React from "react";

// This will render shows that you didn't like or that you haven't finished because you didn't like them

const Disliked = () => {
  return <div>this is the disliked component</div>;
};

export default Disliked;
