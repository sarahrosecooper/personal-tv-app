import React from "react";

// This component will have the tv shows that are still in production that the user is interested in being updated when new episodes/seasons are dropped

const KeepUp = () => {
  return <div>this is the KeepUp component</div>;
};

export default KeepUp;
