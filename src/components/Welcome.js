import React, { useState } from "react";

// Login/Welcome page for users
// FIXME If I am logged in, register LandingPage if I am not logged in,
// register LandingPage
// This will also render the Top5 component
// This will also render some of the Watchlist component

const Welcome = () => {
  return <div>this is the Login/Welcome page component</div>;
};

export default Welcome;
